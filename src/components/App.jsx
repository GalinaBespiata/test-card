import HomePage from 'components/HomePage';

export const App = () => {
  return (
    <div
      style={{
        display: 'flex',
        padding: '20px',
        justifyContent: 'center',
        alignItems: 'center',
        // fontSize: 40,
        color: '#010101',
      }}
    >
      <HomePage />
    </div>
  );
};
